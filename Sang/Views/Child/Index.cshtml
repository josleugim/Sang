@model IEnumerable<Sang.Models.SangChild>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            SangClient
        </th>
        <th>
            Name
        </th>
        <th>
            FirstName
        </th>
        <th>
            LastName
        </th>
        <th>
            CompleteName
        </th>
        <th>
            BirthDate
        </th>
        <th>
            CuestionaryResult
        </th>
        <th>
            RegisterDate
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SangClient.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CompleteName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BirthDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CuestionaryResult)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegisterDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SangChildID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SangChildID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SangChildID })
        </td>
    </tr>
}

</table>
