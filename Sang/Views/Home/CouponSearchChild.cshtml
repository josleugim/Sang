@model IEnumerable<Sang.Models.SangChild>
@{
    ViewBag.Title = "CouponSearchChild";
    Layout = "~/Views/Shared/_AdminMasterPage.cshtml";
}
<div class="evaluacion">
    <br />
    <h2 style="text-align: center">
        Busqueda de cupón</h2>
    @using (Html.BeginForm("CouponSearch", "Home", FormMethod.Post))
    {
        <p style="text-align: center">
            @Html.TextBox("coupon", null, new { style = "background-color: black; border: 1px solid #2F2F2F; color: white; height:25px; font-size: 16px; text-align: center" })
            <input type="submit" value="buscar" />
        </p>
    }
    @using (Html.BeginForm("SleepingImageMenor", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <table style="margin: 0 auto; width: 60%">
            <tr>
                <th>
                    Nombre completo
                </th>
                <th>
                    Código del cupón
                </th>
                <th>
                </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CompleteName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CouponNumber)
                    </td>
                    <td>
                        @Html.ActionLink("Notificar, cupón canjeado", "SleepingImageAsistMenor", new { id = item.SangChildID }, new { style = "font-size: 18px; color: yellow; text-decoration:underline" })
                    </td>
                </tr>
                <tr>
                    <td>
                        <p>
                            @Html.TextBox("identificador", item.SangChildID, new { style = "visibility: hidden" })
                            <input type="file" id="document" name="document" />
                        </p>
                    </td>
                    <td>
                        <input type="submit" value="Cargar documento" />
                    </td>
                </tr>
            }
        </table>
    }
</div>
